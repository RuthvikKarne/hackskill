"""Shared utilities used across all modules.

Contents:
  pagination.py  — PaginationParams, PaginatedResponse
  response.py    — Standard ApiResponse envelope
  validators.py  — Reusable Pydantic validators (phone, UUID, date)
"""
